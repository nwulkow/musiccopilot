# core analysis
librosa>=0.10.2
numpy>=1.26
scipy>=1.11
soundfile>=0.12
scikit-learn>=1.3

# stem separation (htdemucs_6s gives drums/bass/other/vocals/guitar/piano)
demucs>=4.0.1
torch>=2.1

# note transcription (optional but strongly recommended; pYIN fallback otherwise)
# basic-pitch is polyphonic: it handles rhythm/chordal parts. torchcrepe is the
# monophonic tracker used to re-transcribe solos, where a polyphonic model
# invents extra notes and chops bends into steps (see notes.py). It reuses the
# torch install demucs already needs - no tensorflow.
basic-pitch>=0.4.0
torchcrepe>=0.0.24
# resampy (via torchcrepe) still imports pkg_resources, dropped in setuptools 81
setuptools<81

# lyrics: this IS Whisper - a CTranslate2 build of the same models, ~4x faster
# and lighter on memory. lyrics.py falls back to `openai-whisper` (pip install
# openai-whisper) if this one is missing, so you only ever need one of the two.
faster-whisper>=1.0.3

# midi export
pretty_midi>=0.2.10

# Gemini
google-genai>=1.0.0
pydantic>=2.6

# cli output
rich>=13.7

# live play-along and recording (tab --follow, record)
sounddevice>=0.5

# Scriptum, the web front end (python -m scriptum). The client itself is in
# web/ and is built with npm; these are only what serves it and the API.
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
